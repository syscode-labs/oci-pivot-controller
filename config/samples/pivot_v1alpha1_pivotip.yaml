apiVersion: pivot.oci.io/v1alpha1
kind: PivotIP
metadata:
  name: pivotip-sample
  namespace: default
spec:
  # Service in the same namespace to assign the floating public IP to.
  serviceRef:
    name: my-app

  # Optional: restrict eligible nodes to a specific availability domain.
  # nodeSelector:
  #   topology.kubernetes.io/zone: uk-london-1-ad-1

  # Optional: override the controller's default OCI compartment.
  # compartmentId: ocid1.compartment.oc1..aaaa...
