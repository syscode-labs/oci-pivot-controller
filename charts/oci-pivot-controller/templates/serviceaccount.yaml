apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "oci-pivot-controller.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "oci-pivot-controller.labels" . | nindent 4 }}
